# Validation

`schema.json` defines the event contract. V1 generation is typed through the frozen `Event` dataclass; external adapters should add JSON Schema validation, language-code normalization, duplicate reports, and license manifests here.
