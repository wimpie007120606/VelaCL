# Ingestion

External ingestion is intentionally deferred until dataset license and provenance review. The executable V1 generator is `velacl.data`; future adapters must emit the validated event schema without silently changing raw text.
