# Continual-learning evaluation

The experiment records a full task-by-stage macro-F1 matrix, average accuracy, positive forgetting and backward transfer. Metric definitions are fixed in `velacl.metrics.continual_metrics` and tested with known values.
