# Performance evaluation roadmap

Prometheus collects live inference latency and request counts. Hardware-specific P50/P95/P99, throughput, memory and quantization tables must be generated on named deployment hardware rather than checked in as placeholders.
