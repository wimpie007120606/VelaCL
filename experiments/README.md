# Experiment artifacts

`runs/<method>/metrics.json` and `run.json` are portable, inspectable records. Model checkpoints are generated and gitignored due to size; reproduce them with `make experiment`. `registry.json` contains quality-gated aliases and history.
