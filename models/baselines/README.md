# Baselines

Static and naive sequential baselines are implemented by `velacl.experiment` using the same `ByteTransformer` and optimizer settings as replay methods so the controlled comparison changes only the learning strategy.
