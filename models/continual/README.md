# Continual methods

Random replay, active balanced replay, memory balancing and stage orchestration live in `velacl.replay` and `velacl.experiment`. Checkpoints include replay events so resumed runs preserve method semantics.
