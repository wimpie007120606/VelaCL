# Active learning

The selector in `velacl.replay.active_balanced_select` exposes uncertainty, embedding diversity, language scarcity, intent novelty, simulated forgetting risk and safety importance as separate recorded features.
